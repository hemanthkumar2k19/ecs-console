import StatsPanel from "@/components/dashboard/StatsPanel";

export const metadata = {
  title: "Dashboard | ECS Console",
  description: "Evidence Collection System Management Dashboard",
};

export default function DashboardPage() {
  return <StatsPanel />;
}
